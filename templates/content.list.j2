<p>
<h2>{{title}}</h2>
</p>

<div>
  Counters are updated by hx-swap-oob. Yes, this is so cool!
  <span id="skip">
    <div>
      skip = {{skip_next - limit}},
    </div>
  </span>

  <span id="skip_next">
    <div>
      skip_next = {{skip_next}},
    </div>
  </span>
</div>

<p></p>

<div>
  <button class="btn btn-dark" hx-get="/htmx/content.list.tbody?limit={{ limit }}" hx-target="#table-body"
    hx-swap="innerHTML" hx-trigger="click"> Reset
  </button>

  <span id="bluebutton">
    <button class="btn btn-light" hx-get="/htmx/content.list.tbody?skip={{ skip_next }}&limit={{ limit }}"
      hx-target="#table-body" hx-swap="beforeend" hx-trigger="load, click"> Load More
    </button>
  </span>
</div>

<div class="container bd-example">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>id</th>
        <th>name</th>
        <th>email</th>
      </tr>
    </thead>
    <tbody id="table-body">
    </tbody>
  </table>
</div>
